#include <stdio.h>

int main(void) {
    int T;
    scanf("%d", &T);

    while(T--) {
        long long A, B;
        scanf("%lld %lld", &A, &B);
        printf("yes\n");
    }

    return 0;
}